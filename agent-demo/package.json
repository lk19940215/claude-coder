{
  "name": "agent-demo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node --no-deprecation src/agent.mjs",
    "clean": "rm -rf logs/*.log logs/*.json && echo 'logs 已清除'"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "dotenv": "^17.3.1"
  }
}
